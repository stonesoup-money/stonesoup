node_modules/
dist/
.wrangler/
coverage/

# secrets — never tracked
.dev.vars
.dev.vars.*
.env
.env.*
!.dev.vars.example
!.env.example

*.log
.DS_Store

# dispatch pipeline: per-ticket worktrees live inside this checkout,
# and local pipeline config is generated by tooling this repo does not ship
.claude/worktrees/
.claude/settings.local.json
