# Privacy policy claims — evidence table

This table exists so a reviewer (human or agent) can judge the privacy
policy, ToS, and data promise on accuracy, not plausibility (STON-13
plan, "Accuracy: how a reviewer knows it is true, not plausible"). One
row per normative claim in `/privacy`, `/terms`, or `/data-promise` →
evidence, or `NOT YET IMPLEMENTED — STON-n` where the policy describes
committed design for a capability that has not shipped yet.

**Any ticket that changes what data is collected, retained, or
transmitted must update this table and the corresponding document in the
same PR** (AGENTS.md, "Public pages and the privacy policy"). A behaviour
change that outruns this table is a review-invariant violation (AGENTS.md
invariant #24).

## How this table is checked (review round 3, revised round 4)

Three review rounds of spot fixes went 4 major → 3 major → 5 major on
these pages, because each round fixed the untrue claims a reviewer named
and nothing walked every sentence against this table — and because this
table had no row at all for several of the pages' claims, so "check the
table" could not close it either. `docs/legal-claim-ledger.md` was built
to invert that: it enumerates **every block** of all six rendered
outputs (`docs/{privacy,terms,data-promise}.md` and the three
`.hosted.md` variants) and names the row(s) below that a reviewer decided
backs it.

A fourth round found what that mechanism cannot do: prove the row a
block names actually supports it. A fabricated claim pointed at a real,
unrelated row passed every check, because comparing prose to an
implementation is a judgment call, not something a hash or a lookup can
verify. `packages/web/src/legal-claim-coverage.test.ts` now only checks
the ledger's own mechanical integrity as a data structure (it parses,
keys are unique, no entry points at a block that is no longer rendered,
every rendered block has some entry) — it is a drift reminder, not proof
these claims are true.

**The real claim-truth gate is `.github/CODEOWNERS`**, requiring the
repo owner's review of `packages/core/src/legal/**`, this table, the
ledger, and the generated `docs/*.md` before any change to them merges.
That review is where "does this row's evidence actually back this
claim?" gets asked — by a person, every time, not by a test that can be
satisfied by pointing at *any* row.

**The Marker column.** `required` means the prose must carry the marker
at the point of the claim; `placeholder` means the prose must carry the
`[[...]]` unfilled-fact marker; `not required` means the claim is true as
written today. The test that decides which claims need a marker is
*restrictive* vs. *affirmative existence*: "only allowlisted mail is
fetched" is true while nothing runs and needs no marker; "this instance
sets one cookie" is false while nothing runs and always needs one.
A block whose row is `required` may satisfy the marker through the
paragraph a bullet list hangs off, so a list introduced by a marked
sentence does not repeat the marker on every item.

## Human checkbox

- [ ] **A human has re-walked every row below**, confirming each is still
      accurate, before: (a) the hosted service accepts its first real
      user, and (b) Google OAuth verification is submitted. Any row still
      marked `NOT YET IMPLEMENTED` at that point is a launch blocker, not
      a note. Sign-off: <!-- name, date -->.

## Claims

| # | Claim (document) | Status | Marker | Evidence |
|---|---|---|---|---|
| 1 | The app is single-tenant; this instance's D1/R2 hold only your data. (privacy) | Implemented | not required | `wrangler.jsonc` — one `DB` (D1) and `RECEIPTS` (R2) binding per deployed Worker; `migrations/0001_initial_schema.sql` has no cross-tenant table. |
| 2 | Money is stored as whole cents, never reformatted. (privacy) | Implemented | not required | `migrations/0001_initial_schema.sql` — every money column is `*_cents INTEGER` with a `typeof(...) = 'integer'` CHECK; `packages/core/src/money.ts`. |
| 3 | The Anthropic key is stored as a Workers Secret, never in D1, never logged. (privacy, self-hosted/BYOK) | Implemented (storage) / policy commitment (logging) | not required | `packages/worker/src/env.d.ts` (secret-only binding, no `wrangler.jsonc` `vars` entry); `.dev.vars.example`. "Never logged" is enforced by code review (AGENTS.md, BYOK bullet), not a runtime check — no code path logs it today because no such code exists yet. |
| 4 | This app's own Gmail queries are restricted to a sender-domain allowlist the user controls. (privacy) | NOT YET IMPLEMENTED — STON-6 | required | Gmail sync (cron trigger) is unimplemented; `packages/worker/src/index.ts`'s `scheduled()` is a stub. Committed design: AGENTS.md, Pipeline rules, "Gmail sync only fetches sender-domain-allowlisted mail." **This restriction is enforced by this app's query, not by the OAuth grant** — see row 33, which is the other half of the same sentence and must be read with it (review round 3, finding 9). |
| 5 | The initial Gmail backfill window is 90 days. (privacy) | Implemented (config) / NOT YET IMPLEMENTED (sync itself) | required | `packages/core/src/config.ts` `BACKFILL_WINDOW_DAYS = 90`; `wrangler.jsonc` `vars.BACKFILL_WINDOW_DAYS`; cross-checked against the rendered policy text in `packages/worker/src/public/policy-claims.test.ts`. The number is real config; the sync that would honor it is STON-6. |
| 6 | Email bodies are discarded entirely after parse. (privacy, data-promise) | Restrictive — vacuously true, no parser exists | not required | No email-parsing code exists yet. Committed design: AGENTS.md, Privacy section, "Email bodies are discarded entirely after parse." |
| 7 | Gmail data is never transferred to a third party except Anthropic, never used for ads, never human-read without consent. (privacy) | Restrictive — design commitment | not required | No code path exists yet that could violate this (no Gmail integration). Enforced going forward by AGENTS.md review invariants #3, #4, #12 and this table's re-walk requirement. |
| 8 | The data recipients are exactly Anthropic, Cloudflare, Google, and Stone Soup's central submission service. (privacy) | Implemented (Cloudflare) / NOT YET IMPLEMENTED (all three others) — STON-5 / STON-6 / STON-9 | required | `packages/worker/package.json` dependencies (`@anthropic-ai/sdk`, `hono`) — no analytics/ad SDK anywhere in the repo; `wrangler.jsonc` bindings are all first-party Cloudflare services. The submission service has no client anywhere in this repository — see row 21 and `docs/dataset-publication.md`. Review round 1, finding 2: this row previously covered only three of the document's four disclosed recipients. |
| 9 | Golden-set records carry no receipt id, user id, store, or purchase timestamp, and no image reference — context is dropped at write. (data-promise) | Implemented | not required | `migrations/0001_initial_schema.sql`, `golden_set` table definition — no such columns exist. Machine-checked: `packages/worker/src/public/policy-claims.test.ts`, `PRAGMA table_info(golden_set)` assertion (the one read-only exception the STON-13 gate allows). This is the *local-write* boundary — see row 21 for the separate submission-payload boundary. |
| 10 | `labeler` is written and kept locally, pseudonymized at the instance boundary at submission (not at dataset export), never nulled at write. (data-promise) | Implemented (schema, local write) / NOT YET IMPLEMENTED (submission client) — STON-9 | required | `migrations/0001_initial_schema.sql` — `golden_set.labeler TEXT NOT NULL`, machine-checked in `policy-claims.test.ts`. Resolved design: decisions.md, "when is `labeler` pseudonymized?" — pseudonymization happens in the submission client, before anything leaves this instance, not in the separate `open-receipts` publication pipeline. The submission client itself does not exist in this repository (STON-9, human-gated). |
| 11 | Golden-set contribution defaults to "on" in both self-hosted and hosted deployments. (terms, data-promise) | Implemented (the default itself) | not required | `packages/core/src/config.ts` `GOLDEN_SET_CONTRIBUTION_DEFAULT = "on"`; `wrangler.jsonc` `vars.GOLDEN_SET_CONTRIBUTION`; cross-checked in `policy-claims.test.ts`. What the default would *switch on* is row 21; what reads the value is row 49. |
| 12 | The client-side sensitive-string filter runs before any submission leaves the device. (data-promise) | NOT YET IMPLEMENTED — STON-9 | required | The filter is planned as a local, client-side check (STON-9's GO scope); it does not exist in this repository yet. |
| 13 | There is no admin view anywhere; no operator can read a user's data. Each deployment's database is physically separate; receipt images live in storage partitioned per user and enforced in code, not structurally separate. (privacy) | Implemented (by absence) | not required | No admin route exists anywhere in `packages/worker/src` — grep-verifiable. **D1**: one database per deployment (hosted: per-tenant; self-hosted: the operator's own) — a genuinely separate database no other user's instance can reach, not just an access-controlled table. **R2**: hosted deployments use one shared bucket with a per-user key prefix (brief, "Provisioning on signup" — "R2: one bucket, existing per-user prefix"), enforced by application code, not by storage-level tenant isolation. Per-tenant R2 buckets were considered and rejected as an upgrade path: Cloudflare accounts cap at roughly 1,000 buckets, and per-tenant buckets complicate provisioning for no isolation gain worth it at this scale. This row, and the prose it backs, describe today's design, not an aspiration — the D1/R2 distinction is stated because it is true, not glossed over because it reads as weaker. Guarded going forward by AGENTS.md review invariant #12. |
| 14 | This instance sets exactly one cookie, a signed session token, and no tracking cookie. (privacy) | NOT YET IMPLEMENTED — STON-4 | required | Auth/session is unimplemented. Committed design: AGENTS.md, Auth section, "hand-rolled, signed JWT in a cookie." Review round 3, finding 4: the page stated this in the present tense on the document Google's reviewer reads, while `packages/worker/src/public/pages.test.ts` asserts every one of these responses carries *no* `Set-Cookie` — see row 60. |
| 15 | Disconnecting Gmail stops sync; removing this instance's stored OAuth tokens on disconnect is part of the same unbuilt feature. (privacy) | NOT YET IMPLEMENTED — STON-4 / STON-6 | required | No disconnect flow or token-removal code exists yet. See row 24 for token *storage* itself, which is a separate, also-unbuilt piece. |
| 16 | Hosted-mode contact, legal entity, jurisdiction, and effective date are stated, not fabricated. (privacy, terms) | Placeholder, by design | placeholder | `packages/core/src/legal/context.ts` — `DEFAULT_OPERATOR_NAME`, `DEFAULT_OPERATOR_CONTACT`, `PLACEHOLDER_JURISDICTION`, `PLACEHOLDER_EFFECTIVE_DATE` all render as an unmissable `[[...]]` marker until a human sets `OPERATOR_NAME` / `OPERATOR_CONTACT` (`wrangler.jsonc` vars) and edits the jurisdiction/effective-date placeholders directly. A self-hosted deployment never reads any of these — see row 17. `packages/worker/src/public/pages.ts`'s `warnIfHostedIdentityUnfilled()` additionally logs a server-side warning if a hosted deployment is about to serve a request with the operator identity still unfilled (review round 1, finding 4). There is no "hosted deletion mechanism" placeholder — see row 26. |
| 17 | A self-hosted instance needs no legal entity and no jurisdiction, and says so; its operator is whoever deployed it. (privacy, terms) | Implemented | not required | `packages/core/src/legal/documents.test.ts` — "self-hosted mode renders with no unfilled-fact placeholder" (asserts no `[[` marker appears when `DEFAULT_LEGAL_CONTEXT.mode === "self-hosted"`). |
| 18 | `/privacy`, `/terms`, `/data-promise` return 200 with no credentials required, real content, no SPA shell. | Implemented | not required | `packages/worker/src/public/pages.test.ts` (`SELF.fetch`, no auth header, asserts 200 + real body); `scripts/verify-public-routes.mjs` (static `run_worker_first` cross-check, wired into `pnpm check`). Not a prose claim — no document tag, so the coverage test does not require a block to reference it. |
| 19 | Open Receipts is to be released under CC0 with a citation request, committed before any label is collected. (data-promise) | Statement of intent / by construction (no dataset exists) | required | `packages/core/src/legal/data-promise.ts` — the CC0 + citation section. No golden-set write path exists yet (STON-9) and no dataset has been published, so this is a commitment made in advance, not a description of a downloadable artifact (review round 3). |
| 20 | Publication requires a permanent human sample review before every tagged release. (data-promise) | Statement only — the pipeline itself is gated | not required | See `docs/dataset-publication.md`. No release script exists in this repository and no release has been made; this row records the promise, not a mechanism this repo enforces (the mechanism is a human process in the separate `open-receipts` repo). Phrased in the prose as binding the first release rather than describing an existing habit. |
| 21 | No receipt image, receipt ID, account ID, merchant name, or purchase date/timestamp ever leaves this instance through the golden-set path; the real `labeler` never crosses the instance boundary either — a per-instance pseudonym is substituted before submission; and no label leaves at all today. (privacy, terms, data-promise) | Design commitment (schema-backed for the "never written" fields; submission client not yet built) — STON-9 | required | The never-written fields are machine-checked the same way as row 9. The `labeler` pseudonym substitution is committed design with no code in this repository (review round 1, findings 1 & 2; decisions.md's resolved anonymization-boundary section is authoritative). Until the submission client exists, *nothing* reaches the central pot by any path. |
| 22 | The golden-set *submission payload* is an explicit field allowlist distinct from the local `golden_set` row, and that allowlist is exhaustive against the schema. (data-promise) | Machine-checked (allowlist definition and exhaustiveness) / NOT YET IMPLEMENTED (the client that would build a payload from it) — STON-9 | required | `packages/core/src/legal/submission-fields.ts` defines `GOLDEN_SET_SUBMISSION_ALLOWLIST` / `_PSEUDONYMIZED_COLUMNS` / `_EXCLUDED_COLUMNS`; `packages/worker/src/public/policy-claims.test.ts` asserts every real `golden_set` column (via `PRAGMA table_info`) is categorized in exactly one of the three, that the rendered "It contains exactly:" bullets name exactly the allowlist, **and that every pseudonymized column is named in a pseudonym bullet** (review round 3, finding 5: routing a column through `_PSEUDONYMIZED_COLUMNS` previously left the whole suite green with the prose untouched). |
| 23 | `created_at` (a per-label timestamp) does not leave this instance at submission, not even coarsened, unless a coarsened value is explicitly added to the allowlist in the future. (data-promise) | Restrictive — machine-checked as excluded today | not required | `packages/core/src/legal/submission-fields.ts` — `created_at` is in `GOLDEN_SET_SUBMISSION_EXCLUDED_COLUMNS`, asserted by the same exhaustiveness test as row 22. Resolved design: decisions.md, "when is `labeler` pseudonymized?" (a per-label timestamp next to a per-labeler pseudonym is a re-identification handle in a CC0 dataset). |
| 24 | Gmail OAuth tokens are stored in this instance, scoped to your account, revocable at any time. (privacy) | NOT YET IMPLEMENTED — STON-4 / STON-6 | required | `sources` (`migrations/0001_initial_schema.sql`) has `auth_state`, `external_account`, and `gmail_history_id` — no token column exists anywhere in the schema. Contrast: the Gmail *sync checkpoint* (`gmail_history_id`) is real schema today; the *credential* that would let sync actually run is not (review round 1, finding 6). |
| 25 | In hosted mode, receipt extraction runs on the operator's own Anthropic API key, not a key the user provides. (privacy) | NOT YET IMPLEMENTED — STON-5 | required | Committed design: brief, "Compute & keys (BYOK)" — "Hosted tier runs on our Anthropic key with per-user cost tracking; self-hosters BYOK." No extraction code exists. The per-account *metering* half of the old wording is row 45, which is its own unbuilt mechanism and must not ride along on this row (review round 3, finding 1). |
| 26 | There is no account-deletion or per-receipt delete mechanism in this application today, in either self-hosted or hosted mode. (privacy, terms) | Implemented (true by absence) | not required | `migrations/0001_initial_schema.sql` — every foreign key is `ON DELETE RESTRICT`; no delete route exists anywhere in `packages/worker/src` (grep-verifiable). Tracked as STON-18. Addresses review round 1, finding 4. |
| 27 | Nothing in this codebase collects, stores, or transmits receipt, message, or label data today: no sign-in, no upload path, no extraction call, no Gmail sync, no golden-set write, no submission. (privacy, data-promise) | Implemented (true by absence) | not required | `packages/worker/src/index.ts` routes exactly `/api/health`, `/api/byok/status`, and the three legal pages; `scheduled()` and `queue()` are empty; no `golden_set` read/write code exists (STON-13's human gate, `docs/dataset-publication.md`). Grep-verifiable, and the STON-13 gate is what keeps it so. |
| 28 | `docs/privacy-claims.md` is the row-by-row status of every claim on these pages, and every block of the rendered pages maps to a row. (privacy, data-promise) | Implemented | not required | This file, plus `docs/legal-claim-ledger.md` and `packages/web/src/legal-claim-coverage.test.ts` — see "How this table is checked" above. The repository is public, so a reader of the page can actually open the file the page names. |
| 29 | Stone Soup ingests receipts from photo upload and Gmail, extracts line items with a vision model, and categorizes them against a fixed taxonomy. (privacy) | NOT YET IMPLEMENTED — STON-2 / STON-5 / STON-6 / STON-8 | required | None of the pipeline exists: no upload route, no extraction client, no Gmail sync, no taxonomy enumeration beyond `TAXONOMY_VERSION`. This is the page's headline description of the product and it was previously stated in the present tense with no marker anywhere near it (review round 3, the systematic pass). |
| 30 | Receipt images are stored in this instance's R2 bucket at a path scoped to your account. (privacy) | Implemented (bucket binding) / NOT YET IMPLEMENTED (upload path) — STON-2 | required | `wrangler.jsonc` `r2_buckets` — the `RECEIPTS` binding is real; no code writes to it. |
| 31 | Receipt and line-item records are stored in this instance's D1 database. (privacy) | Implemented (schema) / NOT YET IMPLEMENTED (write path) — STON-2 / STON-5 | required | `migrations/0001_initial_schema.sql` — `receipts` and `line_items` exist with their money CHECKs (row 2); nothing writes a row to either. |
| 32 | The Gmail scope this app requests is `gmail.readonly`. (privacy) | NOT YET IMPLEMENTED — STON-4 | required | No OAuth code exists; the scope is committed design (AGENTS.md, Auth: Google OAuth only, one consent flow; brief, "Gmail app stays in testing mode"). Named explicitly rather than described, because row 33 depends on which scope it is. |
| 33 | `gmail.readonly` permits reading any mail in the account; Google does not enforce the sender-domain allowlist — this app's own queries do. (privacy) | Implemented (a true statement about Google's scope, not about this codebase) | not required | Review round 3, finding 9, raised by the STON-4 planner: the page previously said the app requests "only the scope needed … **not your full mailbox**", which tells a reader Google enforces a limit that only this app's code enforces. A user who believes the grant is narrow believes a compromised or changed app still could not read their personal mail; it could. The two facts are now stated separately — this row, and row 4 — because the honest version is the more credible one. |
| 34 | The only outbound request this codebase makes today is the optional Anthropic key check, which sends no receipt, message, or label data. (privacy) | Implemented | not required | `packages/worker/src/byok/validate.ts`, reached only from `GET /api/byok/status`; it sends the configured key and model name to Anthropic and nothing else. No other `fetch` to an external host exists in `packages/worker/src` — grep-verifiable, and the STON-13 gate forbids adding one. |
| 35 | Anthropic receives receipt images and extracted text to run the vision extraction call. (privacy) | NOT YET IMPLEMENTED — STON-5 | required | No extraction client exists; `packages/worker/src/byok/validate.ts` is a key check, not an extraction call. |
| 36 | Cloudflare is this instance's infrastructure (hosting, D1, R2, Queues), not a separate data recipient. (privacy) | Implemented | not required | `wrangler.jsonc` bindings; this page is itself served by the Worker. |
| 37 | Receipt images, records, and the Gmail connection are retained for as long as your account exists on this instance. (privacy) | NOT YET IMPLEMENTED — STON-4 (there are no accounts) | required | No account exists to scope a retention period to, and nothing is stored. The deletion half of the same subject is row 26, which is true by absence today. |
| 38 | A label already contributed could not be recalled after publication. (privacy, data-promise) | Statement of intent — no label has been contributed | not required | Follows from CC0 publication (row 19) and the absence of any submission path (row 21). Stated as what the promise will mean, not as something that has already bitten anyone. |
| 39 | Data in transit is encrypted (HTTPS); data at rest sits in Cloudflare D1/R2 under this instance's own account. (privacy) | Implemented | not required | Cloudflare Workers serve these routes over HTTPS; `wrangler.jsonc` D1/R2 bindings are this deployment's own. |
| 40 | Stone Soup's source is AGPL-3.0; it may be run, modified, and redistributed under those terms, and is provided without warranty. (terms, privacy) | Implemented | not required | `LICENSE` (AGPL-3.0), `package.json` `"license": "AGPL-3.0-only"` in every workspace package; the repository is public. |
| 41 | The hosted service runs an unmodified copy of that same open-source code. (terms) | NOT YET IMPLEMENTED — no hosted service is running | required | Brief, "Hosting architecture": tenant workers run the unmodified open artifact. There is no deployed hosted fleet, so this is a design commitment about a service that does not exist yet (review round 3, finding 8: previously unrowed). |
| 42 | On the hosted free tier, golden-set contribution is the payment and is not optional; running your own instance is the alternative. (terms, data-promise, privacy) | Implemented (this is the deal these terms state, not a capability) | not required | Brief, "Golden-set contribution defaults": hosted free tier — on, required by ToS. Review round 3, finding 3: `/privacy` and `/data-promise` used to send a hosted reader to `/terms` "for how to turn this off", and hosted `/terms` had no opt-out to offer. `data-promise.ts` now renders per deployment mode instead of ignoring its context, so the referral ends somewhere truthful. |
| 43 | There is no paid tier that keeps your labels private. (terms) | Implemented (true by absence) | not required | No billing code, no tier logic, no payment dependency anywhere in the repo; the v1 scope fence (AGENTS.md) bans building one in this run. |
| 44 | Access to the hosted service may be gated by invite code or email allowlist. (terms) | NOT YET IMPLEMENTED — STON-4 | required | Brief, "Invite gating (hosted)". No signup, no invite code, no allowlist exists. Review round 3, finding 1: this is now disclosed as design and is explicitly **not** a ground for termination until it is built — see row 46. |
| 45 | Each hosted account has a monthly extraction budget, and exceeding it produces a "budget used" state rather than a charge. (terms, privacy) | NOT YET IMPLEMENTED — no constant, no config var, no metering code | required | Brief, "Free-tier budget (hosted)": per-tenant monthly extraction cap. `grep -i budget` over `packages/` finds no budget constant and `wrangler.jsonc` has no `*_BUDGET` var; there is no metering code and no extraction to meter. Review round 3, finding 1: a hosted user was told in writing they could not be surprise-billed by a mechanism that does not exist, in a document that then let the operator terminate them for abusing it. |
| 46 | Neither the extraction budget nor invite gating is enforced, and these terms give the operator no right to act against a user over either. (terms) | Implemented (true by absence — the enforcement clauses were removed) | not required | `packages/core/src/legal/terms.ts` — Acceptable Use no longer forbids "circumventing the extraction budget or invite gating" and Termination no longer covers an account that "abuses the extraction budget" or "was created to evade invite gating". Review round 3, finding 1: marking an unbuilt limit while leaving it enforceable is incoherent, not merely unbuilt. Whichever ticket builds one of these limits restores its enforcement clause in the same change. |
| 47 | Acceptable use: personal receipt tracking only; no processing others' receipts without their agreement; no accessing another account's data. (terms) | Restrictive — no service exists to violate | not required | Contract terms, not capabilities. The two that referred to unbuilt mechanisms were removed — see row 46. |
| 48 | The operator may suspend or terminate an account that violates acceptable use. (terms) | Statement (a contract term, not a mechanism) | not required | There is no account system and no suspension mechanism (row 27); this states what the operator may do once there is. Scoped to row 47's list only. |
| 49 | `GOLDEN_SET_CONTRIBUTION` is a real configuration value, but no code reads it yet. (terms, data-promise) | Implemented (the var) / NOT YET IMPLEMENTED (any reader) — STON-9 | required | `wrangler.jsonc` `vars.GOLDEN_SET_CONTRIBUTION`, `packages/core/src/config.ts` `GOLDEN_SET_CONTRIBUTION_DEFAULT`; grep finds no consumer. Setting it to `"off"` today changes nothing, because nothing is contributed either way — which the prose now says rather than implying a working switch. |
| 50 | The `golden_set` table exists in this instance's schema. (data-promise) | Implemented | not required | `migrations/0001_initial_schema.sql`; machine-checked by the `PRAGMA table_info(golden_set)` assertions in `policy-claims.test.ts`. |
| 51 | The local `golden_set` row holds raw line text, merchant type, model guess, verdict, `labeler`, routing reason, versions, `split`, id and `created_at`. (data-promise) | Implemented (schema) / NOT YET IMPLEMENTED (anything writing one) — STON-9 | required | Same PRAGMA assertions as row 50, which check each named column exists. What would write a row is row 64. |
| 52 | The raw line text is submitted exactly as printed, not normalized or paraphrased. (data-promise) | NOT YET IMPLEMENTED — STON-9 | required | Nothing is submitted at all (row 21). Design rationale: a normalized line would train a model on data that does not look like a real receipt. |
| 53 | A skipped item is never sent. (data-promise) | Restrictive — vacuously true, nothing is sent | not required | No review UI (STON-8) and no submission client (STON-9) exist. |
| 54 | Pot: submission payloads stream into a private, central submission database as they are made. (data-promise) | NOT YET IMPLEMENTED — GATED (STON-9's central endpoint) | required | `docs/dataset-publication.md`; AGENTS.md, Human gates. No submission database exists and no code here may talk to one. Review round 3, finding 2: this whole pipeline was stated in the present tense. |
| 55 | Ladle: a fixed public release process deduplicates, re-runs the sensitive-string filter server-side as a second check, assigns splits, computes card statistics, and emits versioned JSONL — as public code in the separate `open-receipts` repository, credentials excluded. (data-promise) | NOT YET IMPLEMENTED — GATED | required | `docs/dataset-publication.md`, "Why this repo has none of this code". The release script is not written, here or anywhere; the `open-receipts` repository is where it will live. A reader who accepts CC0 partly because a second, server-side net re-screens sensitive strings and the release code is auditable today is accepting something that does not exist (review round 3, finding 2). |
| 56 | Table: the publication surface is a tagged GitHub release, never the live submission database. (data-promise) | NOT YET IMPLEMENTED — GATED | required | Same as row 55. No release has been tagged and no database exists to expose. |
| 57 | Nothing stored in this instance leaves it except through the golden-set path described. (privacy) | Restrictive | not required | Row 27 (nothing leaves at all today) plus rows 8 and 21 for the designed recipients. |
| 58 | There is no analytics vendor, advertising or tracking pixel, or third-party OCR/receipt-parsing service. (privacy) | Implemented (true by absence) | not required | `packages/*/package.json` — the only runtime dependencies are `hono` and `@anthropic-ai/sdk`; grep finds no analytics or ad SDK. |
| 59 | Extraction is a single vision-model call, with nothing else touching receipt data. (privacy) | NOT YET IMPLEMENTED — STON-5 | required | AGENTS.md, Pipeline rules: "One extraction contract, two modality nodes… Zero per-merchant parsers." No extraction code exists. |
| 60 | An uncredentialed request for these pages comes back with no `Set-Cookie` header. (privacy) | Implemented | not required | `packages/worker/src/public/pages.test.ts` asserts `set-cookie` is null on every public route response. This is the true half of the sentence whose other half is row 14. |
| 61 | A self-hosted instance's data goes to the deployer's own Cloudflare account, not to the Stone Soup project or its maintainers. (privacy) | Implemented | not required | `wrangler.jsonc` — every binding is the deploying account's own D1/R2/Queues; there is no Stone Soup-operated endpoint in this codebase at all (row 27, row 34). |
| 62 | This app's use and transfer of Google API data adheres to the Google API Services User Data Policy, including Limited Use. (privacy) | Statement of intent — no Google integration exists | not required | Required by Google's OAuth verification; enforced going forward by AGENTS.md invariants #3 and #4 and by rows 4, 6, 7 and 32. |
| 63 | The "last revised" date is when this document's text was last edited. (privacy, terms, data-promise) | Implemented | not required | `packages/core/src/legal/context.ts` `LEGAL_LAST_UPDATED`, validated as an ISO date in `documents.test.ts`. Deliberately not the hosted terms' legal effective date — that is row 16's placeholder. |
| 64 | Confirming or correcting a category writes one `golden_set` row on this instance. (data-promise) | NOT YET IMPLEMENTED — STON-9 | required | No review UI and no golden-set write path exist. The STON-13 gate forbids writing one here; the table it would write to is row 50. |
| 65 | In hosted mode, this service's own Anthropic API key — not one the user provides — is stored as a Workers Secret in the operator's deployment. (privacy) | Implemented (the mechanism) | not required | `packages/worker/src/env.d.ts` — `ANTHROPIC_API_KEY` is a Workers Secret, no `wrangler.jsonc` `vars` entry, read by `GET /api/byok/status` (row 34) regardless of deployment mode; only whose key is configured changes. Review round 4: the Security section previously described this unconditionally as "your Anthropic key... in a self-hosted or BYOK deployment", which dangled for a hosted reader the hosted bullet (row 25) already told has no personal key to store. |

## Notes on the "NOT YET IMPLEMENTED" rows

Writing a narrower privacy policy that simply omits Gmail, or golden-set
contribution, was considered and rejected (STON-13 plan): the Gmail
consent-screen requirement is the entire reason this page exists, and a
policy that doesn't mention Gmail cannot serve as the consent-screen URL
Google actually wants. The policy therefore describes the **committed
design** for capabilities that must exist before any user data can be
collected under them, and this table is what keeps that honest — every
row above either points at real, checkable evidence, or says plainly that
it doesn't yet.
